#FILE CONTAINS VENDOR SPECIFIC DETAILS

vendors:
  cisco:

    ios:
      device_type: cisco_ios
      pre_commands:
        - "terminal length 0"
      backup_commands:
        - "show running-config"

    nxos:
      device_type: cisco_nxos
      pre_commands:
        - "terminal length 0"
      backup_commands:
        - "show running-config"
    
    wlc:
      device_type: cisco_wlc
      pre_commands:
        - "terminal length 0"
      backup_commands:
        - "show running-config"
        
  juniper:
    junos:
      device_type: juniper_junos
      backup_commands:
        - "show configuration | no-more"